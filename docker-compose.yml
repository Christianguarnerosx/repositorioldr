services:
  # Contenedor principal PHP + Composer + Node
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: repositorio-app
    volumes:
      - .:/var/www
    networks:
      - laravel
    command: sh -c "cd /var/www && composer install && chmod -R 775 storage bootstrap/cache && chown -R www-data:www-data storage bootstrap/cache && php-fpm"

  # Node separado para levantar Vite (React)
  node:
    image: node:20
    container_name: repositorio-node
    working_dir: /var/www
    volumes:
      - .:/var/www
    ports:
      - "5173:5173" # Vite dev server
    command: sh -c "npm install && npm run dev -- --host"
    depends_on:
      - app
    networks:
      - laravel

  # Nginx para servir Laravel
  web:
    image: nginx:alpine
    container_name: repositorio-web
    ports:
      - "8000:80" # Laravel backend
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - laravel

  # Base de datos MySQL
  db:
    image: mysql:8.0
    container_name: repositorio-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: repositorio
      MYSQL_USER: adminrepo
      MYSQL_PASSWORD: passrepo
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - laravel

  # phpMyAdmin
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: repositorio-phpmyadmin
    restart: always
    ports:
      - "8080:80"
    environment:
      PMA_HOST: db
      PMA_USER: adminrepo
      PMA_PASSWORD: passrepo
    networks:
      - laravel

volumes:
  db-data:


networks:
  laravel:
    driver: bridge
